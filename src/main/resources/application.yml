server:
  error:
    include-stacktrace: on_param
    include-message: always
spring:
  datasource:
#    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/school?useSSL=false&createDatabaseIfNotExist=true
    username: root
    password: root

  mvc:
    pathmatch:
      matching-strategy: ANT_PATH_MATCHER

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
